{
  "name": "ooo-rules-tests",
  "private": true,
  "description": "Firebase RTDB security-rules tests. `npm test` starts the database emulator (requires Java) and runs the assertions against ../firebase.rules.json.",
  "scripts": {
    "test": "PATH=/opt/homebrew/opt/openjdk/bin:$PATH firebase emulators:exec --only database --project demo-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "firebase-tools": "^13.35.1"
  }
}
