# macOS
.DS_Store

# Dependencies / build output
node_modules/
promo-video/out/

# Firebase emulator debug logs
*-debug.log

# Tot cat-sitting guide — contains street address & key location; kept local
# on purpose, never publish (decision 2026-07-08)
tot.html
tot-guide.html
tot.pdf
css/tot.css
assets/tot/

# Executed plan handoffs
tot-plan.md
stickynotes-plan.md

# Repo audit — documents not-yet-fixed attack vectors; the repo is public
# and served at scottfriedman.ooo, so keep this local
IMPROVEMENTS.md

# Band notes seed content — lyrics, member emails, and the shared password;
# lives only in RTDB (seeded via scripts/seed-bandnotes.mjs), never in the repo
scripts/bandnotes-seed.json
assets/bandnotes/

# Workout-logger golden contract fixtures — personal training data; source of truth is the me repo
tests/fixtures/app-sessions/
